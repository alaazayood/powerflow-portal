# PowerFlow Portal - نظرة عامة على المشروع

## 🎯 نبذة عن المشروع
**PowerFlow Portal** هو نظام متكامل لإدارة التراخيص والعملاء، مصمم لتوفير تجربة مستخدم سلسة وآمنة. يهدف النظام إلى أتمتة عمليات التسجيل، التحقق، وإدارة الاشتراكات للأفراد والشركات، مع لوحة تحكم إدارية قوية.

## 🏗️ معمارية النظام (Architecture)
يعتمد النظام على معمارية **Client-Server** مفصولة تماماً:

- **الواجهة الأمامية (Frontend):** تطبيق صفحة واحدة (SPA) مبني باستخدام React، يتواصل مع الخادم عبر RESTful API.
- **الواجهة الخلفية (Backend):** خادم Node.js يقدم خدمات الـ API، ويدير منطق العمل، ويتصل بقاعدة البيانات.
- **قاعدة البيانات (Database):** قاعدة بيانات علائقية (MySQL) لتخزين البيانات بشكل منظم.

## 🛠 المكدس التقني (Tech Stack)

### Frontend
- **Framework:** React 18
- **Language:** TypeScript
- **State Management:** Redux Toolkit (لإدارة حالة التطبيق والمستخدم)
- **UI Library:** Material UI (MUI) v5 (لتصميم عصري ومتجاوب)
- **Networking:** Axios (للتعامل مع الـ API)
- **Routing:** React Router v6

### Backend
- **Runtime:** Node.js
- **Framework:** Express.js
- **Language:** TypeScript
- **Database Interface:** Knex.js (Query Builder)
- **Validation:** Zod (للتحقق من صحة البيانات المدخلة)
- **Security:** Helmet, CORS, Rate Limiting
- **Authentication:** JSON Web Tokens (JWT)

### Infrastructure & Tools
- **Database:** MySQL 8.0+
- **Email Service:** Resend API (لإرسال أكواد التحقق)
- **Package Manager:** npm

## 📂 هيكلية المشروع (Project Structure)

### Backend (`/backend`)
- **`src/controllers`**: يحتوي على منطق العمل (Business Logic) لكل مسار.
- **`src/routes`**: تعريف نقاط الاتصال (API Endpoints).
- **`src/middlewares`**: برمجيات وسيطة للتحقق من الصلاحيات (Auth) وغيرها.
- **`src/services`**: خدمات مساعدة للتعامل مع قاعدة البيانات أو منطق معقد.
- **`src/utils`**: دوال مساعدة عامة.
- **`src/migrations`**: ملفات إنشاء وتعديل هيكل قاعدة البيانات.

### Frontend (`/frontend`)
- **`src/components`**: مكونات React القابلة لإعادة الاستخدام (مثل الأزرار، النماذج).
- **`src/pages`**: صفحات التطبيق الرئيسية (مثل صفحة التسجيل، لوحة التحكم).
- **`src/store`**: إدارة حالة التطبيق باستخدام Redux.
- **`src/services`**: ملفات الاتصال بالـ API.
- **`src/theme`**: إعدادات التصميم والألوان (Material UI Theme).

## ✅ الميزات الرئيسية (Key Features)

### 1. إدارة المستخدمين والمصادقة
- **تسجيل ذكي:** نموذج تسجيل موحد يدعم الأفراد والشركات مع حقول ديناميكية.
- **تحقق ثنائي:** التحقق من البريد الإلكتروني عبر كود OTP (One-Time Password).
- **أمان:** تشفير كلمات المرور باستخدام Bcrypt، وحماية المسارات باستخدام JWT.

### 2. إدارة التراخيص (Licenses)
- إنشاء وإدارة تراخيص البرمجيات.
- دعم أنواع مختلفة من الاشتراكات (شهري، سنوي).
- تتبع حالة الترخيص (نشط، منتهي، ملغى).

### 3. لوحة التحكم (Dashboard)
- واجهة إدارية لعرض الإحصائيات.
- إدارة العملاء والتراخيص من مكان واحد.

## 🔄 حالة المشروع الحالية
- [x] هيكلية المشروع الأساسية (Backend & Frontend).
- [x] نظام المصادقة (تسجيل، دخول، تحقق).
- [x] قاعدة البيانات والـ Migrations.
- [x] واجهة التسجيل والتحقق.
- [ ] تطوير لوحة التحكم المتقدمة.
- [ ] تقارير وإحصائيات تفصيلية.