# دليل التثبيت والتشغيل الشامل (Deployment Guide)

هذا الدليل مصمم ليتم اتباعه خطوة بخطوة عند تنزيل المشروع كملف مضغوط (ZIP) على جهاز جديد.

---

## 1. التجهيز (Preparation)
1. **فك الضغط:** قم بفك ضغط الملف الذي نزلته من GitHub.
2. **البرامج المطلوبة:** تأكد من وجود البرامج التالية على الجهاز:
   - **Node.js** (يفضل إصدار 18 أو أحدث).
   - **XAMPP** (لتشغيل قاعدة البيانات MySQL).
   - **VS Code** (أو أي محرر نصوص).

---

## 2. إعداد السيرفر (Backend Setup)

1. **الدخول للمجلد:** افتح التيرمنال (CMD أو PowerShell) وادخل لمجلد `backend`:
   ```bash
   cd backend
   ```

2. **ملف الإعدادات (.env):**
   - هذا الملف **غير موجود** في النسخة المنزلة ويجب إنشاؤه يدوياً.
   - أنشئ ملفاً جديداً داخل مجلد `backend` وسمّه `.env` (بدون اسم قبل النقطة).
   - انسخ والصق المحتوى التالي داخله:
     ```env
     # إعدادات قاعدة البيانات (تأكد أن XAMPP يعمل)
     # إذا كان للروت كلمة مرور، ضعها بعد النقطتين: mysql://root:PASSWORD@localhost...
     DATABASE_URL="mysql://root:@localhost:3306/powerflow_portal"
     
     # منفذ السيرفر
     PORT=4000
     
     # مفاتيح التشفير (يمكنك تغييرها)
     JWT_SECRET="my-super-secret-key-123"
     
     # مفتاح خدمة الإيميل (اختياري حالياً)
     RESEND_API_KEY="re_123_test"
     ```

3. **تثبيت المكتبات (npm install):**
   - نفذ الأمر التالي لتنزيل المكتبات:
     ```bash
     npm install
     ```
   - ⚠️ **هام:** إذا ظهرت أخطاء (Errors) باللون الأحمر، جرب هذا الأمر بدلاً منه (يحل مشاكل التعارض):
     ```bash
     npm install --legacy-peer-deps
     ```

4. **تجهيز قاعدة البيانات:**
   - تأكد أن زر **MySQL** في XAMPP لونه أخضر (Start).
   - نفذ الأمر التالي لإنشاء الجداول في قاعدة البيانات:
     ```bash
     npx prisma db push
     ```
   - *إذا نجح، ستظهر رسالة خضراء: `🚀 Your database is now in sync with your Prisma schema.`*

5. **تشغيل السيرفر:**
   ```bash
   npm run dev
   ```
   - *يجب أن ترى: `Server running on port 4000`.*
   - **لا تغلق هذه النافذة!**

---

## 3. إعداد الواجهة (Frontend Setup)

1. **الدخول للمجلد:** افتح نافذة تيرمنال **جديدة** (New Terminal) وادخل لمجلد `frontend`:
   ```bash
   cd frontend
   ```

2. **تثبيت المكتبات:**
   ```bash
   npm install --legacy-peer-deps
   ```
   *(استخدمنا `--legacy-peer-deps` هنا أيضاً لتجنب مشاكل توافق إصدارات React).*

3. **تشغيل الموقع:**
   ```bash
   npm start
   ```
   - *سيفتح المتصفح تلقائياً على الرابط: `http://localhost:3000`.*

---

## 4. حلول سريعة للمشاكل (Troubleshooting)

- **مشكلة: `npm` غير معروف (not recognized)**
  - الحل: تأكد من تثبيت Node.js وأعد تشغيل الجهاز.

- **مشكلة: اتصال قاعدة البيانات (Connection refused)**
  - الحل: تأكد أن XAMPP يعمل، وأن `DATABASE_URL` في ملف `.env` صحيح.

- **مشكلة: أخطاء غريبة أثناء التثبيت**
  - الحل: احذف مجلد `node_modules` وملف `package-lock.json` ثم حاول `npm install --legacy-peer-deps` مرة أخرى.
