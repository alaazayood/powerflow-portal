# دليل الإعداد والتشغيل (Setup Guide)

هذا الدليل يشرح كيفية إعداد بيئة التطوير لمشروع PowerFlow Portal وتشغيله محلياً.

## ⚙️ المتطلبات المسبقة (Prerequisites)
تأكد من تثبيت الأدوات التالية على جهازك:
- **Node.js**: الإصدار 16 أو أحدث (يفضل LTS).
- **MySQL**: خادم قاعدة البيانات (الإصدار 8.0 أو أحدث).
- **Git**: لإدارة النسخ.
- **VS Code**: (مستحسن) كمحرر أكواد.

---

## 🚀 خطوات التثبيت (Installation Steps)

### 1. استنساخ المستودع (Clone Repository)
```bash
git clone <repository_url>
cd powerflow-portal
```

### 2. إعداد قاعدة البيانات (Database Setup)
1. قم بالدخول إلى MySQL وإنشاء قاعدة بيانات جديدة:
```sql
CREATE DATABASE powerflow;
```

### 3. إعداد الواجهة الخلفية (Backend)

انتقل إلى مجلد الـ backend:
```bash
cd backend
```

قم بتثبيت الاعتماديات:
```bash
npm install
```

قم بإنشاء ملف `.env` ونسخ الإعدادات من `env.example`:
```bash
cp env.example .env
```

**تعديل ملف `.env`:**
افتح ملف `.env` وتأكد من صحة بيانات الاتصال بقاعدة البيانات:
```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=powerflow
JWT_SECRET=your_super_secret_key_change_it
RESEND_API_KEY=re_123456_your_resend_key
PORT=4000
```

تشغيل الـ Migrations لإنشاء الجداول:
```bash
npx knex migrate:latest
```

تشغيل الخادم (Development Mode):
```bash
npm run dev
```
> يجب أن تظهر رسالة: `Server running on port 4000`

### 4. إعداد الواجهة الأمامية (Frontend)

افتح نافذة طرفية جديدة (New Terminal) وانتقل إلى مجلد الـ frontend:
```bash
cd frontend
```

قم بتثبيت الاعتماديات:
```bash
npm install
```

(اختياري) إنشاء ملف `.env` إذا لزم الأمر، ولكن الإعدادات الافتراضية عادة ما تكون كافية للتطوير المحلي. تأكد من أن الـ Proxy في `package.json` أو إعدادات الـ API تشير إلى `http://localhost:4000`.

تشغيل التطبيق:
```bash
npm start
```
> سيفتح التطبيق تلقائياً في المتصفح على الرابط: `http://localhost:3001`

---

## 🧪 استكشاف الأخطاء وإصلاحها (Troubleshooting)

- **مشكلة الاتصال بقاعدة البيانات:**
  - تأكد من أن خدمة MySQL تعمل.
  - تأكد من صحة اسم المستخدم وكلمة المرور في ملف `.env`.

- **مشكلة CORS:**
  - تأكد من أن الـ Backend يسمح بالطلبات من `http://localhost:3001` (تم إعداده في `server.ts`).

- **مشكلة في تثبيت الحزم (npm install):**
  - حاول حذف مجلد `node_modules` وملف `package-lock.json` ثم أعد التثبيت.